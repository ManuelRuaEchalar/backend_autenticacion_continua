flask>=3.0,<4.0
tensorflow>=2.15,<3.0
# Keras 2. El modelo FedPer de `mejor.py` se entrenó con TF_USE_LEGACY_KERAS=1;
# Keras 3 cambia el orden de model.weights y la API del optimizador, lo que
# invalidaría los pesos y rompería la conversión a TFLite. Ver app/tf_env.py.
tf-keras>=2.15
numpy>=1.24,<2.0
flwr>=1.8.0
boto3>=1.34.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.29
